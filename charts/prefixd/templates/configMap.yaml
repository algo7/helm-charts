apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "prefixd.fullname" . }}
  labels:
    {{- include "prefixd.labels" . | nindent 4 }}
data:
  feeds.txt: |
    {{- default "" .Values.config.feeds | nindent 4 }}
