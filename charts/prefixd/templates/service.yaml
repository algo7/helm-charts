apiVersion: v1
kind: Service
metadata:
  name: {{ include "prefixd.fullname" . }}
  labels:
    {{- include "prefixd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prefixd.selectorLabels" . | nindent 4 }}
